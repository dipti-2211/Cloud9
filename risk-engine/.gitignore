# Derived rasters produced by precompute_layers.py (keep intermediate rasters ignored)
data/_dist_to_road_utm.tif

# Source data files — large binaries, keep out of git
data/*.nc
data/*.pbf
data/.gitkeep

# Explicitly un-ignore runtime data rasters and ML model
!data/*.tif
!data/*.pkl

# Python virtualenv
.venv/
__pycache__/
*.pyc
*.pyo
